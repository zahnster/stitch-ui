{
  "name": "stitch-ui",
  "version": "0.1.0",
  "description": "Stitch - minimalist, configurable component library",
  "main": "index.js",
  "author": "Jade Pennig <jade@pennig.name>",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "start:storybook": "pnpm dev --filter @stitch-ui/components",
    "build": "pnpm build --filter @stitch-ui/components",
    "start": "pnpm start --filter @stitch-ui/components",
    "dev:website": "pnpm dev --filter @stitch-ui/website",
    "build:website": "pnpm build --filter @stitch-ui/website",
    "start:website": "pnpm start --filter @stitch-ui/website"
  }
}
